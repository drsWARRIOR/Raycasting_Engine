#include <Window.h>

// Entry point of the program
int main(int argc, char* argv[])
{
	// Launch the main window and run the application loop
	// Title: "Raycaster"
	// Window position arguments (if needed by RunWin) - SDL3 typically doesn't require positions
	// Window width and height constants
	RunWin("Raycaster", WIN_WIDTH, WIN_HEIGHT);
}